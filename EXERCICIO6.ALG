Algoritmo "exercicio6"

Var
   vel, m, vm: real


Inicio
      escreval("digite a velocidade do véiculo")
        leia(vel)

      vm <- 434.4 / vel
      m <- vel / 3.6
      
         escreval("o percuso de são paulo até o rio de janeiro durará", vm, "hs")
            escreval("velocidade em m/s:", )
Fimalgoritmo